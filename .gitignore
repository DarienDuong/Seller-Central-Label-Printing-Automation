node_modules/
.env
.auth/
output/
traces/
*.log
.DS_Store

# Real batch files copied from the example (real SKUs/quantities, not secrets
# but not repo content either) — keep products.example.json tracked.
data/*.json
!data/products.example.json
